name: Test Linux Build

on:
  workflow_dispatch:

jobs:
  build-electron-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install

      - name: Install Chromium for E2E
        run: pnpm --filter @dm-hero/app exec playwright install --with-deps chromium

      - name: Run narrative E2E tests
        run: pnpm test:e2e

      - name: Upload E2E artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            packages/app/playwright-report/
            packages/app/test-results/
          if-no-files-found: ignore

      - name: Build Nuxt
        run: pnpm --filter @dm-hero/app build

      - name: Rebuild better-sqlite3 for Electron
        run: pnpm --filter @dm-hero/app electron:rebuild

      - name: Build Electron app
        working-directory: packages/app
        run: pnpm exec electron-builder --linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: List build output
        run: ls -la packages/app/dist-electron/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dm-hero-linux-test
          path: packages/app/dist-electron/*.AppImage
          if-no-files-found: warn
